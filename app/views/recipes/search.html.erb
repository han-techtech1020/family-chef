<div class="bg-white p-6 rounded-xl shadow-sm border border-orange-100">
  <h2 class="text-xl font-bold text-gray-800 mb-4 text-center">👨‍🍳 何を食べたい気分ですか？</h2>

  <!-- generateアクションに向けてデータを送るフォーム -->
  <!-- method: :get にすることで、URLにパラメータを含めます -->
  <%= form_with url: generate_recipes_path, method: :get, local: true, class: "space-y-6" do |f| %>
    
    <div>
      <%= f.label :user_request, "リクエスト（任意）", class: "block text-sm font-bold text-gray-700 mb-1" %>
      <%= f.text_area :user_request, placeholder: "例：和風であっさりしたもの\n例：賞味期限切れの牛乳を使い切りたい\n例：子供が喜ぶハンバーグ", class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-orange-500 focus:ring focus:ring-orange-200 py-3 h-32" %>
      <p class="text-xs text-gray-400 mt-2">※何も入力しない場合は、在庫と栄養バランスから自動で提案します。</p>
    </div>

    <div>
      <%= f.submit "AIシェフに注文する 🍳", class: "w-full bg-gradient-to-r from-orange-400 to-orange-600 text-white font-bold py-4 rounded-xl shadow-lg hover:opacity-90 transform transition active:scale-95 cursor-pointer text-lg" %>
    </div>

  <% end %>

  <div class="text-center mt-6">
    <%= link_to "やめる", root_path, class: "text-gray-400 text-sm hover:text-gray-600" %>
  </div>
</div>