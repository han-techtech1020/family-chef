<div class="bg-white p-6 rounded-xl shadow-sm border border-orange-100">
  <h2 class="text-xl font-bold text-gray-800 mb-6 text-center">📝 家族情報の編集</h2>

  <!-- フォーム（newと同じものを使えます） -->
  <%= form_with model: @family_member, local: true, class: "space-y-5" do |f| %>
    <!-- (new.html.erb のフォームの中身をここにコピペしてください) -->
    <!-- 名前、ステータス、アレルギーの入力欄 -->
    
    <div class="pt-4">
      <%= f.submit "更新する", class: "w-full bg-orange-500 text-white font-bold py-3 px-4 rounded-xl shadow-lg hover:bg-orange-600 transition cursor-pointer" %>
    </div>
  <% end %>

  <div class="text-center mt-6">
    <%= link_to "キャンセル", family_members_path, class: "text-gray-400 text-sm hover:text-gray-600" %>
  </div>
</div>