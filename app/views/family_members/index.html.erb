<h1>家族の体調・ステータス管理</h1>

<div style="margin-bottom: 20px;">
  <%= link_to "🏠 冷蔵庫に戻る", root_path %> | 
  <%= link_to "＋ 家族を追加する", new_family_member_path %>
</div>

<table border="1">
  <thead>
    <tr>
      <th>名前</th>
      <th>現在の状態（AIへの指示）</th>
      <th>アレルギー情報</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    <% @family_members.each do |member| %>
      <tr>
        <td><%= member.name %></td>
        <td><%= member.status %></td>
        <td><%= member.allergy_info %></td>
        <td>
          <%= link_to "編集", edit_family_member_path(member) %> |
          <%= link_to "削除", family_member_path(member), data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>