<div class="min-h-[80vh] flex flex-col justify-center">
  <div class="bg-white p-8 rounded-2xl shadow-lg border border-orange-100">
    
    <!-- タイトル -->
    <div class="text-center mb-8">
      <h2 class="text-3xl font-bold text-orange-500 mb-2">おかえりなさい 👨‍🍳</h2>
      <p class="text-gray-400 text-sm">Family Chefへログイン</p>
    </div>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-6" }) do |f| %>
      
      <!-- メールアドレス -->
      <div>
        <%= f.label :email, class: "block text-sm font-bold text-gray-700 mb-1" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-orange-500 focus:ring focus:ring-orange-200 py-3 px-4" %>
      </div>

      <!-- パスワード -->
      <div>
        <%= f.label :password, class: "block text-sm font-bold text-gray-700 mb-1" %>
        <%= f.password_field :password, autocomplete: "current-password", placeholder: "••••••", class: "w-full border-gray-300 rounded-lg shadow-sm focus:border-orange-500 focus:ring focus:ring-orange-200 py-3 px-4" %>
      </div>

      <!-- ログイン維持チェックボックス -->
      <% if devise_mapping.rememberable? %>
        <div class="flex items-center">
          <%= f.check_box :remember_me, class: "h-4 w-4 text-orange-500 focus:ring-orange-400 border-gray-300 rounded" %>
          <%= f.label :remember_me, "ログインしたままにする", class: "ml-2 block text-sm text-gray-600" %>
        </div>
      <% end %>

      <!-- ボタン -->
      <div>
        <%= f.submit "ログイン", class: "w-full bg-orange-500 text-white font-bold py-3 px-4 rounded-xl shadow-lg hover:bg-orange-600 transition transform active:scale-95 cursor-pointer" %>
      </div>
    <% end %>

    <!-- リンク集（新規登録など） -->
    <div class="mt-6 text-center space-y-2">
      <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
        <%= link_to "新規登録はこちら", new_registration_path(resource_name), class: "text-sm text-orange-500 hover:text-orange-700 font-bold" %><br />
      <% end %>
      
      <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
        <%= link_to "パスワードを忘れましたか？", new_password_path(resource_name), class: "text-xs text-gray-400 hover:text-gray-600" %>
      <% end %>
    </div>

  </div>
</div>